# Logging

[`GroupedLog`][usbip.grouplog.GroupedLog] is the small logging helper the bundled
examples use to print grouped, readable USB activity.

::: usbip.grouplog
    options:
      members:
        - GroupedLog
